## Summary
- Remove the admin "시스템(System)" page (`app/admin/system/page.tsx`) and its API route (`app/api/admin/system/route.ts`) — external-tool shortcuts + minor status only, no shared logic.
- Remove the corresponding "시스템" nav entry from `AppHeader.tsx` (admin header) and `app/admin/page.tsx` (dashboard quick-nav). Admin nav now has 6 items.
- `/admin/system` and `/api/admin/system` no longer exist as routes (confirmed absent from `next build` route table).

## Scope
Only 4 files touched (2 deleted, 2 modified) — no other admin page, nav item, config, migration, or test changed.

## Test plan
- [x] `grep -rn "admin/system" app lib tests` — clean (no references)
- [x] `grep -rn "\"시스템\"" app` — clean
- [x] `npx eslint .` — 0 problems
- [x] `npx tsc --noEmit` — exit 0 (after clearing stale `.next/` cache referencing the deleted routes)
- [x] `npm run build` — succeeds; route table confirms `/admin/system` and `/api/admin/system` are gone
- [x] `npx vitest run` — 60 test files / 263 tests, all passing
- [x] `git diff --stat origin/main..HEAD` — exactly the 4 in-scope files

## PR-body checklists (per 제품조립매뉴얼 v1.0)
R1-R5 / D1-D5: N/A — no repo/folder structure change, file deletion + array-element edit only.
B1-B5: N/A — no deploy config change; branch not merged/pushed pending CEO approval.
M1-M6: N/A — no DB/migration change.
V1-V4: N/A — no new UI block added; existing block/nav item removed only.
E1-E6: N/A — no secret/env variable touched.

## Notes
- Branch `fix/remove-admin-system`, created off latest `origin/main` (86b23ec). Not pushed, not merged — CEO approval gate per repo rules.
- Unrelated pre-existing working-tree changes (an itda2 session-log edit, several untracked itda1 session logs, docs screenshots, `supabase/.temp/*`) were present before this task started and were left untouched/uncommitted, out of scope.

🤖 Generated by itda2 (Claude Code)
